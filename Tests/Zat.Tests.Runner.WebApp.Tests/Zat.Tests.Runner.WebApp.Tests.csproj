<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		<TargetFramework>net10.0</TargetFramework>
		<IsTestProject>true</IsTestProject>
	</PropertyGroup>

	<ItemGroup>
		<PackageReference Include="bunit" Version="1.35.3" />
	</ItemGroup>

	<ItemGroup>
		<ProjectReference Include="..\..\Zat.Tests.Runner.WebApp\Zat.Tests.Runner.WebApp.csproj" />
	</ItemGroup>

	<ItemGroup>
		<!-- The shipped settings are what the application reads at startup, so they are tested rather
         than described a second time here. -->
		<None Include="..\..\Zat.Tests.Runner.WebApp\appsettings.json" Link="appsettings.json" CopyToOutputDirectory="PreserveNewest" />
	</ItemGroup>

</Project>
